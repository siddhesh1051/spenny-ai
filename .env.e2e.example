# Copy to .env.e2e and fill for full-flow e2e (login → add expense → transactions).
# If not set, only auth-page e2e tests run; full-flow tests are skipped.
E2E_TEST_EMAIL=your-test-user@example.com
E2E_TEST_PASSWORD=your-test-password

# Optional: override app URL (default: http://localhost:5173)
# PLAYWRIGHT_BASE_URL=http://localhost:5173
